OPENAI_API_KEY=sk-your-key-here
PORT=3847
API_TOKEN=dev-token-change-me
STORAGE_DIR=../../storage

# OPENAI_API_KEY is optional — users can provide their own key in extension Settings.
# When unset, clients must send X-OpenAI-Key on each request that uses OpenAI.
